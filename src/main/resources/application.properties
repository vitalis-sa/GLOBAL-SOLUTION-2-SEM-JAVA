quarkus.datasource.db-kind=oracle
quarkus.datasource.username=rm561489
quarkus.datasource.password=271202
quarkus.datasource.jdbc.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl
quarkus.datasource.jdbc.driver=oracle.jdbc.OracleDriver
quarkus.http.port=8080

# --- Configura��o do CORS ---

# Habilita o filtro CORS
quarkus.http.cors.enabled=true

# --- CORRE��O AQUI ---
# Adiciona a nova URL do Vercel, separada por v�rgula.
# (� uma boa pr�tica remover a barra "/" no final da URL)
quarkus.http.cors.origins=http://localhost:5173

# Permite os m�todos que seu frontend ir� usar
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS

# Permite que o frontend envie headers como 'Content-Type' (para JSON)
quarkus.http.cors.headers=accept,content-type

# Aumenta o timeout do cliente REST (substitua pelo nome exato da sua classe ou pacote se necessário)
# Configuração global para todos os rest clients (mais fácil)
quarkus.rest-client.connect-timeout=60000
quarkus.rest-client.read-timeout=60000

# OU, se quiser ser específico para o BurnoutApiClient:
# "br.com.vitalis.service.BurnoutApiClient" é o nome completo da interface (package + class)
quarkus.rest-client."br.com.vitalis.service.BurnoutApiClient".url=https://equilibrium-pythonandai-906663117168.us-west1.run.app
quarkus.rest-client."br.com.vitalis.service.BurnoutApiClient".connect-timeout=60000
quarkus.rest-client."br.com.vitalis.service.BurnoutApiClient".read-timeout=60000